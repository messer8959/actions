name: Eight workflow
on: [push]
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Contexts and Empressions
        id: context-expression
        run: | 
          echo '${{ tojson(github) }}'
          echo '${{ tojson(runner) }}'
          echo '${{ tojson(job) }}'
          echo '${{ tojson(steps) }}'

